spring:
  application:
    name: product-sale

  r2dbc:
    url: r2dbc:postgresql://ep-fancy-frog-ait4v8pt-pooler.c-4.us-east-1.aws.neon.tech/product_sale_db?sslmode=require&channel_binding=require
    username: neondb_owner
    password: npg_oC1uAFIyb5SK
    pool:
      initial-size: 2
      max-size: 10
      max-idle-time: 30m
      validation-query: SELECT 1

  webflux:
    base-path: /

  data:
    r2dbc:
      repositories:
        enabled: true

server:
  port: ${SERVER_PORT:9098}

# URLs de microservicios externos (API Gateway en la nube)
microservices:
  patients:
    url: ${PATIENTS_SERVICE_URL:https://lab.vallegrande.edu.pe/sigrc/gateway}
  users:
    url: ${USERS_SERVICE_URL:https://lab.vallegrande.edu.pe/sigrc/gateway}
  products:
    url: ${PRODUCTS_SERVICE_URL:https://lab.vallegrande.edu.pe/sigrc/gateway}

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
    enabled: true
    operations-sorter: method
    tags-sorter: alpha

logging:
  level:
    root: INFO
    pe.edu.vallegrande.sigrc.product_sale: DEBUG
    org.springframework.web: DEBUG
    org.springframework.r2dbc: DEBUG
    io.r2dbc.postgresql: DEBUG
    org.springframework.data.r2dbc: DEBUG
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} - %msg%n"
