spring:
  application:
    name: product
  
  r2dbc:
    url: r2dbc:postgresql://neondb_owner:npg_JALrG9Tl6Fjx@ep-fancy-union-avz0w25h-pooler.c-11.us-east-1.aws.neon.tech/prs04product?sslmode=require&channel_binding=require
    username: neondb_owner
    password: npg_JALrG9Tl6Fjx
    pool:
      initial-size: 10
      max-size: 20
      max-idle-time: 30m
      validation-query: SELECT 1
  # Configuración WebFlux
  webflux:
    base-path: /

server:
  port: ${SERVER_PORT:8086}

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
    enabled: true
    operations-sorter: method
    tags-sorter: alpha
  show-actuator: true

# Logging detallado
logging:
  level:
    root: INFO
    pe.edu.vallegrande.sigrc.product: DEBUG
    org.springframework.web: DEBUG
    org.springframework.r2dbc: DEBUG
    io.r2dbc.postgresql: DEBUG
    org.springframework.data.r2dbc: DEBUG
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} - %msg%n"

